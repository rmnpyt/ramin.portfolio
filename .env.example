# Root-level .env — read by Docker Compose for variable substitution in docker-compose.yml
# Copy to .env and fill in your values:  cp .env.example .env

# Your domain (used in Traefik labels and nginx server_name)
DOMAIN=raminshirani.com
